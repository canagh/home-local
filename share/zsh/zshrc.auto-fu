#!/usr/bin/zsh

if [ -f ~/local/share/zsh/auto-fu.zsh/auto-fu.zsh ] ; then
    source ~/local/share/zsh/auto-fu.zsh/auto-fu.zsh
    function zle-line-init () {
        auto-fu-init
    }
    zle -N zle-line-init
    zstyle ':completion:*' completer _oldlist _complete
fi
