#!/usr/bin/env python
import sys
import argparse
parser = argparse.ArgumentParser()
parser.add_argument('string', type=str, nargs='*')
args = parser.parse_args()
sys.stdout.write(' '.join(map(lambda x: str(ord(x)),''.join(args.string))))
