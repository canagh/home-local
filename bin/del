#!/bin/sh

which_q() {
    which "$@" >/dev/null 2>&1
}

if which_q trash-put; then
    exec trash-put "$@"

elif which_q rmtrash ; then
    exec rmtrash "$@"

else
    echo 'please install a tool to use trash' >&2
    exit 1

fi
